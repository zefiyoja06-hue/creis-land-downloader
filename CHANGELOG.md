# 更新记录

## 0.6.5

- 识别 CREIS `meta + Base64 data` 加密响应，自动切换到网页原生“下载为表格”。
- 保留原生下载文件中的完整项目、地块和价格字段，并继续自动改名归档。
- 接口响应按 `landId` 绑定当前任务，避免相邻地块响应串入。
- 出让资料 ZIP 直接保存到 `CREIS土地出让资料`，同名地块加入编号。
- 点击前重新校验按钮文字和坐标，避开“添加收藏”和“添加对比”。
- 修复 XLSX 容器格式、任务取消后的迟到点击和后台恢复超时。

## 验证

```powershell
node .\tools\validate.js
node .\tools\regression.js
```
